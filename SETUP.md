# Quick Setup Guide

## ขั้นตอนการติดตั้งแบบย่อ

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Environment Variables

แก้ไขไฟล์ `.env`:

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxxxx...
VITE_GOOGLE_MAPS_API_KEY=AIzaSyxxxxx...
VITE_THUNDER_API_URL=https://api.thunder-solution.com/verify
VITE_LINE_LIFF_ID=1234567890-xxxxx (optional)
```

### 3. สร้าง Storage Bucket

ใน Supabase Dashboard > Storage:

1. สร้าง bucket ชื่อ `payments`
2. ตั้งค่าเป็น Public
3. เพิ่ม policies:

```sql
CREATE POLICY "Allow authenticated uploads"
ON storage.objects FOR INSERT TO authenticated
WITH CHECK (bucket_id = 'payments');

CREATE POLICY "Allow public to view"
ON storage.objects FOR SELECT TO public
USING (bucket_id = 'payments');
```

### 4. อัปเดต Store Settings

```sql
UPDATE store_settings SET
  store_name = 'ร้านของคุณ',
  store_latitude = 13.7563,
  store_longitude = 100.5018,
  line_notify_token = 'your_token',
  thunder_api_key = 'your_key'
WHERE id = '00000000-0000-0000-0000-000000000001';
```

### 5. สร้าง Admin User

```sql
UPDATE profiles
SET role = 'admin'
WHERE line_user_id = 'your_line_user_id';
```

### 6. รันโปรเจกต์

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:5173`

## API Keys ที่ต้องใช้

### Google Maps API
1. ไปที่ https://console.cloud.google.com/
2. เปิด Maps JavaScript API และ Geocoding API
3. สร้าง API key

### Thunder Solution API
1. สมัครที่ https://thunder-solution.com
2. รับ API key สำหรับตรวจสอบสลิป

### LINE Notify (Optional)
1. ไปที่ https://notify-bot.line.me/
2. สร้าง token สำหรับส่งการแจ้งเตือน

### LINE LIFF (Optional)
1. ไปที่ https://developers.line.biz/
2. สร้าง LIFF app
3. รับ LIFF ID

## Testing

### สำหรับการทดสอบ Thunder API

หากไม่มี API key สามารถทดสอบระบบโดย:

1. ปิดการตรวจสอบ Thunder API โดยไม่ต้องใส่ `thunder_api_key` ใน store_settings
2. ระบบจะข้ามการตรวจสอบและให้อัปเดตสถานะด้วย Admin แทน

### สำหรับการทดสอบ LINE LIFF

หากไม่มี LIFF ID:

1. เว้น `VITE_LINE_LIFF_ID` ว่างหรือใช้ค่า default
2. ระบบจะสร้าง mock user ให้ทดสอบอัตโนมัติ

## หน้าจอต่างๆ

- **หน้าหลัก** - แสดงสินค้าทั้งหมด
- **ตะกร้า** - จัดการสินค้าที่เลือก
- **ออเดอร์** - ดูประวัติการสั่งซื้อ
- **Admin** - จัดการร้านค้า (เฉพาะ admin)
- **โปรไฟล์** - ข้อมูลผู้ใช้

## คำแนะนำ

1. **ใช้ Sample Products** - มี 12 รายการสินค้าตัวอย่างพร้อมใช้งาน
2. **ทดสอบทีละขั้นตอน** - ทดสอบแต่ละฟีเจอร์ก่อนใช้งานจริง
3. **ตรวจสอบ Activity Logs** - ดูประวัติการทำงานของระบบเป็นประจำ
4. **Backup Database** - สำรองข้อมูลก่อนทำการอัปเดตใหญ่

## เริ่มต้นใช้งาน

1. รันโปรเจกต์
2. เข้าหน้าแรก (จะสร้าง user profile อัตโนมัติ)
3. อัปเดต role เป็น admin ใน database
4. Refresh หน้า
5. เข้าหน้า Admin เพื่อตั้งค่าร้าน
6. เริ่มรับออเดอร์!

## ปัญหาที่พบบ่อย

**แผนที่ไม่โหลด?**
- ระบบจะเปลี่ยนเป็นให้พิมพ์ที่อยู่เองอัตโนมัติ

**สลิปไม่ผ่าน?**
- ตรวจสอบยอดเงินต้องตรงกับยอดสั่งซื้อ

**ไม่ได้แจ้งเตือน LINE?**
- ตรวจสอบ LINE Notify token ใน store_settings

## Next Steps

หลังจากติดตั้งเรียบร้อย:

1. เพิ่มสินค้าของตัวเอง (หรือแก้ไข sample products)
2. ตั้งค่าพิกัดร้านให้ถูกต้อง
3. ทดสอบสั่งซื้อจริง
4. เชื่อม LINE LIFF สำหรับ Production
5. ตั้งค่า LINE Notify สำหรับรับแจ้งเตือน

---

**Happy Selling! 🛒**
